#include "sort.h"

void insertion_sort_list(listint_t **list)
{
	listint_t *current;
	int i, j, a_prev, a_next, b_prev, b_next;

	current = list;
	for (i = 0; current; i++)
	{
		for (j = i; j > 0; j--)
		{
			
		}
	}
}
