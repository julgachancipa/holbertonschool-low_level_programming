#include "holberton.h"
/**
 * _print_rev - prints a string rev.
 * @s : pointer-parameter
 * Return: ntg.
 */
void _print_rev(char *s)
{
	int i;
	int sz = 0;

	
	for (i = 0; i >= 0; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
